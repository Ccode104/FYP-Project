# Run lint-staged for staged files only (continue on error to allow commit)
npx lint-staged || echo "⚠️  lint-staged had issues, but continuing..."

# Run full test suite (non-blocking: failures are logged but do NOT block commit)
echo "Running full test suite..."
cd backend && npm run lint && npm test && cd ../frontend && npm run lint && npm test || echo "⚠️  Lint/tests failed, but commit is allowed (pre-commit is non-blocking)."
